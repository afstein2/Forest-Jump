class Platformer2 extends Phaser.Scene {
    constructor() {
        super("platformerScene2");
    }
    // rest is the same but point to a different tilemap
    // this.map = this.add.tilemap("platformer-level-2", ...)
}